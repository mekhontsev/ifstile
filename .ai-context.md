# AI Context: IFStile Project

## Critical Rules for AI Assistants

### 1. Never create files in cmake build directories
`CMAKE_BINARY_DIR` is a temporary build directory — never create or edit source files there.
All source files belong in the project root and its subdirectories (`SrcCore/`, `Src/`, etc.).
